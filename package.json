{
  "name": "demand-planner-full",
  "version": "1.0.0",
  "description": "Full-stack demand planning application with React frontend and Node.js backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm ci --production=false && npm run build",
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\""
  },
  "engines": {
    "node": "18.x",
    "npm": ">=9.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": [
    "demand-planning",
    "inventory",
    "planning",
    "react",
    "nodejs",
    "sqlite",
    "express"
  ],
  "author": "Demand Planner Team",
  "license": "MIT"
}
